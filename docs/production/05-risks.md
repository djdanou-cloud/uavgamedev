# F. Risk Management & De-risking Matrix

Probability: L (< 20 %), M (20–50 %), H (> 50 %). Impact: 1 (days) – 5 (project). Reviewed at every gate; an indicator that trips outside a gate is raised in the weekly 15-minute planning slot. Category: T technical, G gameplay, P production, S store.

| id | risk | cat | prob | impact | measurable early-warning indicator | mitigation | owner | trigger milestone |
|----|------|-----|------|--------|-------------------------------------|------------|-------|-------------------|
| R-01 | GDScript sim performance ceiling on the low device | T | H | 5 | `tools/bench_runner.gd` on device: p95 tick > 2.5 ms at 250/300/60 (soft) / > 3.3 ms (hard) | SoA stores, precomputed tables, spatial hash, view-only tracers (A-04…A-07); bench card CAD-FP-041 before any view work; kill criteria C.2 | H (decision), A (optimisation cards) | FP |
| R-02 | Allocation creep from agent-written code (per-frame Array/Dictionary/String/Callable/Node) | T | H | 4 | `MEMORY_STATIC` growth > 5 % over the 20-min soak; `OBJECT_COUNT` delta > 0 across 1,800 bench ticks; gdlint custom check `tools/check_alloc_patterns.py` flags `Array()`, `[]`, `{}`, `str(`, `%`, `func(` lambdas, `.new()` inside `_process`/`step`/`update` bodies | STD-SIM/STD-VIEW constraint sets on every card; review checklist item; allocation probe in CAD-FP-041 | A (probe), H (audit) | FP, every gate |
| R-03 | Agent context saturation or sudden abandonment leaving a card half-done | P | H | 3 | handoff `last_updated` older than 24 h while status IN_PROGRESS; `tools/handoff_check.py` weekly report | Handoff protocol (H2): update before edits, after each test run, on saturation; WIP push at every checkpoint; takeover prompt; metric "takeover success rate" ≥ 80 % | A | FP onward |
| R-04 | Godot Android export / Gradle / SDK breakage on engine or Play updates | T | M | 4 | CI `android-debug` red on `main` without a code change; Play Console warning email about target API / 16 KB | Pin Godot 4.7.2 + SHA-256 (A-01, A-32); freeze points A-43; human-local export as bypass; `[VERIFY]` items resolved at FP-065 and E-AL-07 | H | FP-065, AL, RC |
| R-05 | Play policy changes (target API, 16 KB, Data safety, ads, testing rules) | S | H | 3 | Play Console policy deadline banner; quarterly check of the target-API page recorded in `docs/release/policy-log.md` | Calendar entries at 2027-08-31; RC checklist G.5; ads rewarded-only keeps policy surface small | H | AL, RC |
| R-06 | Thermal throttling on the low device collapses fps after 10 min | T | M | 3 | soak: minute-20 p95 > 1.10 × minute-1 p95; battery temp > 42 °C | 30 fps cap on low tier (`Engine.max_fps` = 30 when `OS.get_processor_count() ≤ 8 and RAM ≤ 4 GB` heuristic `[HUMAN decides]`), speed governor drops 3× → 2× when sim ms > budget for 2 s, reduced tracer TTL on low tier | A (governor), H (measure) | FP Gate 3, PA soak |
| R-07 | Touch UX on small screens (mis-taps, unreachable controls, unreadable icons) | G | M | 4 | VS playtest: > 90 s to first wave; > 2 mis-placements per BUILD phase; any icon < 22 dp | B.13 sizes; 48 dp targets; drag-to-place with ghost + confirm; inspector as bottom sheet on < 6" screens; playtest at VS | H | VS |
| R-08 | Balance failure: saturation feels hopeless | G | M | 4 | harness "Layered-optimal" wave-30 win rate < 40 %; district damage per wave > 100 median after wave 15 | budget curve knobs (B.6); wave quality bonus; repair cap; tuning loop DES-4 | H | VS, PA, BE |
| R-09 | Balance failure: defence feels trivial / spam wins | G | M | 4 | "SAM-spam" or "AAA-spam" wave-12 win rate > 30 %; exchange ratio < 0.5 for a spam policy | cost-exchange table B.4 as tests; decoys/jammers/SEAD punish single-layer defences; upkeep | H | VS, PA |
| R-10 | Roster scope creep (new threat/emplacement classes mid-production) | P | M | 3 | any card touching `CadEnums.ThreatClass`/`EmpKind` outside a planned epic | A-26 counts fixed; stretch classes have kill dates; new class = ADR + epic, never a card | H | all |
| R-11 | Agent code drifting from the architecture (Nodes in sim, signals in sim, view mutating sim, untyped code) | T | H | 4 | `tools/check_sim_purity.gd` failures; typecheck failures; review finds > 1 architecture violation in 10 cards | CI purity + typing gates; AGENTS.md forbidden patterns; review checklist; abandon rule | A (CI), H (review) | FP onward |
| R-12 | Solo-developer availability and burnout | P | H | 5 | review queue > 10 cards; a week with 0 h logged in `ai/metrics/human_hours.csv`; 3 consecutive weeks below 2 h | 2 h cap is a rule (C.3); milestones slip, scope cuts per kill criteria; no "catch-up weekends" | H | all |
| R-13 | Save corruption / migration failure | T | M | 4 | any `*.corrupt-*.json` produced in QA loops; migration fixture test red | atomic write (A-19); schema versioning; 50-cycle kill/restore loop in VS DoD; corrupt files moved aside, never deleted | A (code), H (audit) | VS |
| R-14 | Asset IP (AI raster art, unlicensed audio, real insignia/names) | S | M | 5 | licence register row missing (CI `licences` job); any file under `art/` not in `AUTHORED.txt` or `LICENSES.md`; a content review finds a real manufacturer/flag/conflict name | §4.4 rule: code-authored vector or human-altered only; fictional naming glossary `docs/design/fiction.md`; CI register check | H | every gate, RC |
| R-15 | Ad SDK: plugin unmaintained, 16 KB/API breakage, consent bugs, policy rejection | S/T | M | 3 | spike gate misses; plugin repo no release for > 6 months; Play "Ads" policy warning | A-24 fallback = no ads at 1.0; rewarded-only; consent via UMP; ad ids injected at RC by the human | H | PA, AL |
| R-16 | Test framework/CI flakiness (headless gdUnit4, runner image changes) | T | M | 2 | > 1 flaky run in 20; CI time > 10 min | pin gdUnit4 and gdtoolkit; retry policy = none (flakes are bugs); GUT fallback (A-08) | A | FP |
| R-17 | Determinism breaks (golden test mismatch) after a sim change | T | M | 3 | `test_determinism.gd` red | rules D2.5; hash per wave points to the first divergent wave; agents may not "update the golden" without a human-approved reason in the PR | A/H | FP onward |
| R-18 | Play closed-test requirement not met (testers drop below 12 before day 14) | S | M | 4 | Play Console tester count < 12 at any daily check | recruit 20, start early (Alpha), daily count in `docs/release/closed-test.md` | H | AL |
| R-19 | Store rejection for content rating / fictional-warfare depiction | S | L | 3 | IARC questionnaire answers flagged; policy warning on review | fictional factions/equipment; no real conflicts; rating "Teen"-class violence declared honestly; no gore | H | RC |
| R-20 | Godot `[VERIFY]` assumptions wrong (setting names, notification values, CLI flags) | T | M | 2 | first card that uses the item fails; agent reports mismatch in handoff | every `[VERIFY]` is resolved by the first card that touches it and recorded in A.0; agents never guess a replacement — they stop and report | A/H | FP |
| R-21 | Review latency starves agents (cards wait > 1 week) | P | H | 2 | queue age > 7 days | queue capped at 8 open PRs; agents pick only cards whose dependencies are merged; batch reviews by workstream | H | all |
| R-22 | Install size or memory over budget from atlases/audio | T | L | 2 | CI APK > 90 MB; RSS > 300 MB on low device | A-20/A-33 budgets; OGG q5 → q4; single atlas | A | BE, RC |
| R-23 | Human realism authority vs. fun (over-realistic kill chain frustrates players) | G | M | 3 | playtesters report "I couldn't tell why it didn't shoot" > 2 of 12 | every non-engagement has an on-screen reason glyph (link lost / no track / out of range / min value); ADR per realism decision with a fun counter-argument | H | VS, BE |
