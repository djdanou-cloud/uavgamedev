# A. Assumptions Register & Locked Decisions

Written 2026-09-08. Every decision the brief left open is here with its rationale and the trigger that reopens it. Cards cite rows as `A-nn`. Nothing in this file is re-argued inside a card; a card that needs a different value files a change request to this file first (human approves).

## A.0 Verified facts (checked 2026-09-08)

| Fact | Value | Source / status |
|------|-------|-----------------|
| Current stable Godot | **4.7.2** (2026-08-18); 4.7.1 on 2026-07-13 | godotengine.org download archive; docs "stable" = 4.7 |
| Android export toolchain (Godot 4.7 docs) | OpenJDK 17; Android SDK: platform-tools ≥35.0.0, build-tools 35.0.1, platform 35, cmdline-tools latest, NDK r28b 28.1.13356709, CMake 3.10.2.4988404 | docs.godotengine.org/en/stable/tutorials/export/exporting_for_android.html |
| Release keystore env vars | `GODOT_ANDROID_KEYSTORE_RELEASE_PATH`, `GODOT_ANDROID_KEYSTORE_RELEASE_USER`, `GODOT_ANDROID_KEYSTORE_RELEASE_PASSWORD` | same page |
| AAB requires Gradle build | yes (`gradle_build/use_gradle_build=true`, `gradle_build/export_format=1`) | same page; class EditorExportPlatformAndroid |
| CLI flags | `--headless`, `--import`, `--export-debug <preset> <path>`, `--export-release <preset> <path>`, `-s/--script`, `--check-only`, `--install-android-build-template`, `--quit`, `--path` | docs command_line_tutorial (4.7) |
| Google Play target API | New apps and updates must target **API 36 (Android 16)** from **2026-08-31**; extension possible to 2026-11-01; existing apps must target ≥35 to stay visible on newer devices | support.google.com/googleplay/android-developer/answer/11926878 |
| 16 KB page size | Required since **2025-11-01** for apps with native code targeting Android 15+; Godot ≥4.5 non-.NET templates are 16 KB aligned (NDK r28b, PR #106358); every plugin `.so` must also be aligned | Play policy; godotengine/godot PR 106358 |
| Android vitals bad-behavior thresholds | user-perceived crash rate **1.09 %** (overall) / **8 %** (per device); user-perceived ANR **0.47 %** / **8 %** | Play Console help 9844486 |
| Closed-test requirement | Personal developer accounts created after 2023-11-13: **12 testers opted in for 14 continuous days** per app before production access; 2026: Play also checks testers actually used the app | Play Console help 14151465 |
| Developer account | $25 one-time fee; identity verification (government ID + payment method under legal name) for new personal accounts; organization accounts need D-U-N-S | Play Console help 13628312 `[VERIFY the exact verification steps at CAD-FP-006]` |
| gdUnit4 CLI | `res://addons/gdUnit4/bin/GdUnitCmdTool.gd`; flags `-a/--add`, `-i/--ignore`, `-c/--continue`, `-conf/--config`, `-rd/--report-directory`, `-rc/--report-count`, `--ignoreHeadlessMode` (mandatory with `--headless`); exit codes 0 pass / 100 failures / 101 warnings; reports `results.xml` (JUnit) + `index.htm` | godot-gdunit-labs.github.io/gdUnit4 |
| gdtoolkit | 4.5.0 on PyPI (`gdlint`, `gdformat`); major version must match Godot major | pypi.org/project/gdtoolkit `[VERIFY 4.7-syntax coverage at CAD-FP-002]` |
| AdMob plugins for Godot 4 | `poingstudios/godot-admob-plugin` (nightlies as of 2026-08-25, Godot 4.2+, rewarded supported) and `godot-sdk-integrations/godot-admob` (unified GDScript API) | GitHub `[VERIFY 16 KB alignment + Google Mobile Ads SDK version at the Pre-Alpha ad spike]` |
| Godot 4.7 highlights relevant here | Android "GABE" standalone build environment, VirtualJoystick node, gyroscope input, HDR output (not Android) | godotengine.org/releases/4.7 `[VERIFY whether GABE removes the JDK/SDK install requirement; plan assumes the classic SDK path]` |
| Release download source | **`godotengine/godot-builds`** releases, tag `<version>-stable`; the Linux CI asset is `Godot_v4.7.2-stable_linux.x86_64.zip`, templates `Godot_v4.7.2-stable_export_templates.tpz` | GitHub release asset list, checked 2026-09-08 (CAD-FP-005) |
| Checksums | Releases publish **`SHA512-SUMS.txt`** only — there is no `SHA256-SUMS.txt` (404). CI verifies with `sha512sum -c` on the single grepped line | same source; corrects the SHA-256 wording in A-32 and §D5.2 |
| GDScript warning settings | VERIFIED on 4.7.2: `untyped_declaration`, `inferred_declaration`, `unsafe_property_access`, `unsafe_method_access`, `unsafe_cast`, `unsafe_call_argument`, `return_value_discarded` are engine-declared (hint `Ignore,Warn,Error`). **`exclude_addons` does not exist in Godot 4** — it is replaced by `debug/gdscript/warnings/directory_rules`, a Dictionary defaulting to `{"res://addons": 0}` (0 = Exclude, 1 = Include) | `ProjectSettings.get_property_list()` dump + behavioural probes, 2026-09-08 (CAD-FP-005); 52 `debug/gdscript/*` settings exist in total |
| Typing gate is real | `--check-only -s <script>` exits **1** and prints `Parse Error: … (Warning treated as error.)` for an untyped `var`, a discarded return value, and a method call on `Variant` | probed on 4.7.2, 2026-09-08; CI asserts this as an inverted test |
| Godot imports every `.csv` as a translation | any `.csv` under `res://` is picked up by the CSV Translation importer and logs `WARNING: Locale '<column>' does not contain any translation`. Directories holding non-translation CSVs need a `.gdignore` file (`ai/`, `docs/`, `data/csv/`, `test/fixtures/csv/`); a CSV that must ship at runtime (`ui/strings/`, CAD-FP-047) needs an `.import` with `importer="keep"` or a different extension | observed on 4.7.2, 2026-09-08 (CAD-FP-005) |
| gdlint config | the `.gdlintrc` option names in A-09 are accepted by gdtoolkit 4.5.0 and enforced (`class-name`, `function-name` violations → exit 1) | probed locally, 2026-09-08 |
| gdUnit4 | **6.2.1** (2026-08-20) works on Godot 4.7.2: suite passes exit 0, a failing suite exits 100, JUnit `results.xml` + `index.html` written, 0 orphans, 2 s wall time. Releases carry no assets — vendored from the source zip. Its fluent assertions require `@warning_ignore_start("return_value_discarded")` in test suites | installed and run locally, 2026-09-08 (CAD-FP-003) |
| Windows package ids | `GodotEngine.GodotEngine` 4.7.2, `EclipseAdoptium.Temurin.17.JDK` 17.0.20.101, `Google.PlatformTools` 37.0.1 | `winget search --source winget`, 2026-09-08 (CAD-FP-001); second independent confirmation that 4.7.2 is current stable |

Facts NOT confirmed (marked in the plan wherever used): Godot 4.7 default `gradle_build/min_sdk` / `target_sdk` values; exact names/values of `debug/gdscript/warnings/*` project settings; integer values of `NOTIFICATION_APPLICATION_PAUSED` (2015) / `_RESUMED` (2014) / `NOTIFICATION_WM_GO_BACK_REQUEST` (1007); editor settings file name `editor_settings-4.7.tres`; whether Godot auto-generates the debug keystore; `OS.get_cmdline_user_args()` availability (believed 4.1+); Android device reach of arm64-only + API 24+.

## A.1 Locked decisions

| Id | Decision | Value | Rationale | Revisit trigger |
|----|----------|-------|-----------|-----------------|
| A-01 | Engine version | Godot **4.7.2** stable; pinned in `project.godot` `config/features=PackedStringArray("4.7","GL Compatibility")`, in CI `GODOT_VERSION=4.7.2`, and in AGENTS.md. Patch upgrades (4.7.x) allowed at milestone boundaries only; minor upgrades (4.8) require a dedicated spike card | Latest stable at time of writing; 16 KB and API 35/36 support present | A 4.7.x patch fixes a bug we hit; 4.8 stable released ≥ 8 weeks before Beta; Play requirement not met by 4.7 templates |
| A-02 | Renderer | **Compatibility (`gl_compatibility`)** for both `rendering/renderer/rendering_method` and `.mobile` | 2D game; GLES 3.0 devices ≈ all active Android devices; lower driver variance on 2019 budget GPUs (Adreno 610 / Mali-G52); no Vulkan shader-compile stutter | Gate-3 bench shows the Compatibility 2D batcher cannot hold 60 fps at ceilings on mid device while a Mobile-renderer spike does |
| A-03 | Sim tick | **30 Hz** fixed (`CadConst.TICK_DT = 1/30`), speed = 0 (tactical pause) / 1 / 2 / 3 ticks per 1/30 s of wall time, hard clamp `MAX_TICKS_PER_FRAME = 6` | 30 Hz gives ≥ 3 ticks per 100 m of SRBM travel at 1,400 m/s, enough for engagement windows; halves cost versus 60 Hz; 3× speed = 90 ticks/s must fit the low-end 10 ms sim budget | `cad_bench_sim` p95 tick time > 3.3 ms on low-end at 250/300/60 ceilings after two optimisation cards |
| A-04 | Entity storage | **Hybrid**: threats and interceptors in struct-of-arrays `Packed*Array`s with free lists (`CadThreatStore`, `CadInterceptorStore`); emplacements and districts as pooled `RefCounted` objects (≤ 64 / ≤ 16) | Hot loops over 500–1,500 rows: Packed arrays avoid per-entity method dispatch and are allocation-free; emplacements are few and stateful (doctrine, ammo, links) and read far better as objects for agents | Bench shows object stores ≤ 1.3× slower than SoA at ceilings (then simplify to objects) |
| A-05 | Gun engagements | AAA/CIWS bursts resolve **instantly by Pk roll**; tracers are **view-only** (`CadTracerView`, TTL ≤ 0.6 s). Only guided interceptors are sim projectiles | Keeps sim projectile count ≤ 600 while the screen shows thousands of tracers; matches how bursts are assessed | Human realism review rejects instant bursts (then add a 0.3–0.8 s time-of-flight delay, still no projectiles) |
| A-06 | Entity ceilings (sim) | Design cap **200 concurrent threats** (wave generator queues beyond); engine ceilings: **mid 400 threats + 600 interceptors + 60 emplacements at 60 fps; low 250 + 300 + 60 at 30 fps**; view: 3,000 tracer/trail instances | Sizing from GDScript throughput estimates (≈ 3–6 M simple packed-array ops/s on low-end); design cap leaves 25 % headroom on low-end | Bench p95 misses budget (see D6 thresholds) |
| A-07 | Collision / proximity | Custom uniform-grid **spatial hash**, cell **500 m**, counting-sort rebuild every tick; no PhysicsServer2D bodies or Area2D for bulk entities | Query radii 1.5–20 km over a 20 × 12 km map; 40 × 24 cells; rebuild is O(n) and allocation-free | Query cost > 25 % of tick time in profile |
| A-08 | Test framework | **gdUnit4 6.2.1** (verified on 4.7.2, A.0), vendored under `addons/gdUnit4/` by CAD-FP-003; `addons/gdUnit4/VERSION.txt` records the source URL and zip SHA-256 | Parameterised tests (Pk matrix, radar equation tables), orphan-node reporting (leak audit), JUnit XML, official CLI with documented exit codes | Headless run flakes > 1 in 20 CI runs → switch to GUT (`gut_cmdln.gd`) via one migration card |
| A-09 | Lint/format | gdtoolkit **4.5.0** pinned: `gdlint` + `gdformat --check`; config `.gdlintrc` (max-line-length 100, max-file-lines 400, function-name snake_case, class-name PascalCase, private prefix `_`) | Only FOSS GDScript linter; CI gate 1 | gdformat rejects a 4.7 syntax we need → per-file `# gdformat: off` region with a logged exception |
| A-10 | Static typing settings | `debug/gdscript/warnings/untyped_declaration=2`, `unsafe_method_access=2`, `unsafe_property_access=2`, `unsafe_cast=2`, `unsafe_call_argument=2`, `return_value_discarded=2`, `inferred_declaration=1`, and `directory_rules={"res://addons": 0}`; 2 = Error, 1 = Warn. **Verified on 4.7.2** (A.0); the earlier `exclude_addons` key was a Godot 3 name and is gone | Brief §5; addons excluded via directory_rules so gdUnit4 does not fail the gate | gdUnit4 upgrade breaks under our settings (then keep `exclude_addons=true`, never lower ours) |
| A-11 | Reference devices | **Low**: 2019 budget phone, 3 GB RAM, Adreno 610 or Mali-G52 class, 720p–1080p; **Mid**: 2022–23 phone, 6 GB, Adreno 6xx–7xx or Mali-G6x, 1080p+. Exact models recorded by human in `docs/devices.md` (CAD-FP-008) | Brief §5 | Play pre-launch report or vitals show a device family with > 8 % crash/ANR |
| A-12 | Min Android version / ABI | `gradle_build/min_sdk` = **24** (Android 7.0) `[VERIFY 4.7 default]`; `gradle_build/target_sdk` = **36** set explicitly `[VERIFY 4.7 default; Play requires 36 from 2026-08-31]`; ABI **arm64-v8a only** | Reach target ≥ 90 % of active devices: Android 7+ ≈ 99 %, arm64 ≈ 98 % `[VERIFY in Play Console device catalog after first upload]`; halves native lib size | Device catalog shows arm64-only reach < 90 % |
| A-13 | Package name | `com.djdanou.cad` | Reverse-DNS of the developer's handle; `com.example.*` rejected by Play; immutable after first upload | Human owns a domain before first Play upload (then use it) — must be final at CAD-FP-065 |
| A-14 | Viewport & stretch | `display/window/size/viewport_width=1920`, `viewport_height=1080`, `stretch/mode="canvas_items"`, `stretch/aspect="expand"`, orientation `display/window/handheld/orientation=4` (sensor_landscape) | Landscape only, 16:9 to 21:9 without letterboxing; UI anchored to safe area | Tablet (4:3) layouts break a gate check (tablets must not break) |
| A-15 | UI scale | `root.content_scale_factor` = clamp(screen_dpi / 160 × 0.5, 0.75, 1.5) computed in `CadUiScale` from `DisplayServer.screen_get_dpi()`; minimum touch target **48 dp** (≈ 8 mm); icon glyph **26 dp** (≈ 4.1 mm) | Brief: legible at ~4 mm; one-thumb reach | Readability gate fails on the low-end 720p device |
| A-16 | Interpolation | Sim stores `prev_x/prev_y`; views lerp with alpha = accumulator / TICK_DT; Godot physics interpolation not used | Sim is not physics; alpha from the accumulator is exact | — |
| A-17 | RNG | `CadRng` wrapping 3 `RandomNumberGenerator` instances (streams WAVEGEN, COMBAT, MOTION) seeded `seed + stream`; VFX randomness in view uses its own RNG, never the sim's | Independent streams keep wave composition stable when combat rolls change; replay = seed + command log | — |
| A-18 | Determinism scope | Bit-exact on the same build and CPU architecture (CI golden tests on x86-64 Linux); cross-device replays are diagnostic only. Sim avoids `pow/sin/cos` in tick loops (radar ranges precomputed per def pair at init) | GDScript floats are IEEE-754 but libm transcendentals differ per platform | A golden test flakes across CI runner images |
| A-19 | Save format | JSON in `user://saves/campaign_<id>.json`, `schema_version` field, migrations table in `CadSaveMigrations`; atomic write = `user://saves/<name>.json.tmp` then `DirAccess.rename_absolute` `[VERIFY overwrite semantics on Android/Windows]`; saves happen at **wave boundaries only** (BUILD phase entry) | In-wave snapshots would need full entity serialisation; process death mid-wave restarts the wave from its BUILD state — acceptable loss ≤ 4 min | Playtest feedback ranks lost-wave-on-kill as a top-3 complaint |
| A-20 | Texture compression | Sprite atlases: **Lossless** (`compress/mode=0`) with `mipmaps/generate=true`, SVG imported at `svg/scale=2.0`; large terrain textures: VRAM compressed (`compress/mode=2`) with `rendering/textures/vram_compression/import_etc2_astc=true` | Flat vector icons show ETC2 block artefacts at 4 mm; one 2048² RGBA atlas = 16 MB VRAM, within budget | RSS > 350 MB on low device |
| A-21 | Atlas strategy | In-engine builder `tools/atlas_build.gd` (headless): `Image.load_svg_from_string(svg, 2.0)` → `Image.blit_rect` into a 2048² atlas → PNG + `CadAtlasMap.tres` (frame rects by `sprite_id`); MultiMesh shader reads frame from `INSTANCE_CUSTOM` | No external tool, agents can run it; deterministic packing order (sorted ids) | > 1 atlas needed (then add `atlas_index` to `CadAtlasMap`) |
| A-22 | Fonts | One OFL font family (candidate: Inter, OFL-1.1) `[VERIFY licence file shipped in art/fonts/]`, sizes 14/18/24/32 dp | Free, legible, tabular numerals for HUD counters | Readability gate |
| A-23 | Audio | Buses: Master → Music, SFX, UI; pooled `AudioStreamPlayer2D` (32) + `AudioStreamPlayer` (8); per-SFX concurrency cap (max 4 same clip in 100 ms); `AudioStreamInteractive` + `AudioStreamSynchronized` for layered music `[VERIFY present in 4.7; added 4.3]`; formats: OGG Vorbis for music, WAV 16-bit for SFX < 1 s | Brief §5 | Music memory > 20 MB |
| A-24 | Ad SDK | **AdMob** via `poingstudios/godot-admob-plugin` (fallback `godot-sdk-integrations/godot-admob`); decision made by the Pre-Alpha ad spike (epic E-PA-06) after a 16 KB + API 36 + UMP consent check; **rewarded video only**; placements: "Emergency resupply" (+1 full reload of one launcher, once per wave), "Salvage boost" (+25 % salvage next wave), "Second chance" (restore integrity to 20 %, once per run); no interstitials, no banners | Both plugins are FOSS and maintained; rewarded-only keeps the store listing simple and avoids ad-driven UX complaints | Spike fails the gate twice → 1.0 ships without ads (kill criterion, 02) |
| A-25 | Analytics/network | none; `permissions/*` all false; only the ad SDK's own network use, which is declared in Data safety | Brief | — |
| A-26 | Content counts 1.0 | 3 maps × 30 waves + endless; threat classes: recon, FPV, OWA, cruise, SRBM, decoy, jammer (7) + SEAD loitering munition and glide bomb as **Beta stretch** (kill criteria in 02); 15 emplacement kinds; 25 tech nodes in 3 tiers | Brief §3; stretch classes reuse existing systems (anti-radiation targeting, mid-alt profile) | Beta kill criterion |
| A-27 | Meta-progression | Tech points (TP): +1 per wave cleared, +10 per scenario cleared; persist per campaign in the save; base roster available without tech: search radar, FCR, passive sensor, AAA, SHORAD, EW jammer, C2, depot, repair; tech-gated: MRSAM (T1, 5 TP), LRSAM (T2, 8 TP), CIWS, hardening, decoy emitter, interceptor UAV (T2) | First run on map 1 reaches ≈ wave 10–12 and funds MRSAM; 4–6 runs to clear map 1 | Balance harness: "Layered-optimal" policy needs > 6 runs to clear map 1 |
| A-28 | Difficulty | One default difficulty + endless mode; no difficulty selector at 1.0 | Scope; balance harness tunes one curve | Playtest win-rate outside 40–70 % band at wave 30 for skilled testers |
| A-29 | Tutorial | No separate tutorial map; waves 1–2 of map 1 carry contextual hint cards (max 6) driven by `CadHintRules`; skippable | Scope; 2 h/week | Closed-test testers fail to start a wave within 3 minutes (observed) |
| A-30 | SRBM impact economics | SRBM impact = 400 damage (district destroyed); district repair capped at 50 hp per wave without repair crews; integrity loss counts toward the loss condition | Makes ABM coverage a real decision instead of a mandatory purchase | `[HUMAN]` realism-vs-fun decision at the VS balance review |
| A-31 | Branching | Trunk-based: `main` protected (CI required, human merge only); one short-lived branch per card `card/CAD-FP-012-threat-store`; squash-merge; WIP commits allowed on card branches (required at each handoff checkpoint) | Agents never touch `main`; handoff durability via pushed branches | — |
| A-32 | CI | GitHub Actions free tier, `ubuntu-latest`; Godot binary + templates downloaded from `godotengine/godot-builds` releases, pinned by version and verified against the release's `SHA512-SUMS.txt`; no third-party marketplace actions except `actions/checkout`, `actions/cache`, `actions/setup-python`, `actions/setup-java`, `actions/upload-artifact` | Supply-chain minimalism (commandment 1 applied to CI) | A first-party Godot action becomes official |
| A-33 | Git LFS | Off; binary assets budgeted ≤ 30 MB in repo (atlases as PNG ≤ 6 MB, audio ≤ 20 MB) | Brief: LFS only if > 50 MB | Repo binaries > 50 MB |
| A-34 | Human time budget | 2 h/week = 60 min card reviews (≈ 5 cards × 12 min), 45 min gate/device work, 15 min planning/metrics | Brief | Review minutes per card average > 15 (then shrink LOC ceiling to 80 for that task type, H4) |
| A-35 | Agent tooling | Agent-agnostic (any coding agent that can read the repo and run shell commands); `AGENTS.md` is the single entry point, `CLAUDE.md` is a byte-identical copy | Continuity does not depend on one vendor | — |
| A-36 | Localization | English only; all user-facing strings in `ui/strings/cad_strings_en.csv` loaded into a `Dictionary[StringName, String]` at boot (no `tr()`) | Brief; cheap 1.1 localisation path | 1.1 |
| A-37 | Accessibility | Palette validated for deuteranopia/protanopia (hostile vs friendly differ in luminance ≥ 40 % and shape); no colour-only information | Cheap, store-review positive | — |
| A-38 | Manual fire override | Tap launcher → tap threat → one salvo ignoring doctrine, 3 s cooldown per launcher | Brief core loop | VS touch playtest |
| A-39 | Time scale | Godot `Engine.time_scale` never used; `get_tree().paused` only for lifecycle/menus; game speed lives in `CadSimDriver` | Timers/tweens in UI stay real-time | — |
| A-40 | Godot .NET/C#, GDExtension | Not used; no `.gdextension` files in the repo | Brief; 16 KB and toolchain simplicity | Sim ceiling kill criterion at FP triggers a `[HUMAN]` decision on a GDExtension spike (would violate commandment 1 by design) |
| A-41 | Physics | `physics/common/physics_ticks_per_second=30`; no physics bodies; `_physics_process` unused in gameplay code | Removes physics-server cost; sim runs in `_process` with its own accumulator | — |
| A-42 | Map coordinates | 1 unit = 1 m; city centre at (0,0); x east, y south (Godot 2D); map 1 = 20,000 × 12,000 m; view scale 0.096 px/m at full-map zoom | Ranges in the design bible are literal metres | — |
| A-43 | Godot version freeze points | Engine patch version may change only at: FP gate, VS gate, Alpha gate; frozen from Beta to Launch | Avoids re-validating export/Play compliance mid-hardening | Security fix or Play compliance forces a change during freeze (human decision) |
